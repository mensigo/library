---
title: Assert
layout: note.njk

upd_date: 2026-02-05

tg_desc: python-assert
tg_pub_time: 2026-02-05
---

## Интро

Окей, есть такая инструкция `assert` для проверки выражения на истинность: если проверка накрылась, бросается `AssertionError` + печатается кастомное сообщение (если указано).


## Моменты

### 1. Assert можно **полностью отключить**

При запуске python скрипта с флагом `-O` (optimize) или под PYTHONOPTIMIZE=1 (непустым) все assert удаляются из кода.

Когда Python интерпретирует код с `-O`, он компилирует файл и сохраняет `.pyc` файл без assert. Это ускоряет выполнение, так как код просто пропускается.

Поэтому `assert` НЕ должен использоваться для проверки пользовательского ввода или критичных ошибок - только для отладки и тестирования.


### 2. Assert в тестах

В юнит-тестах (pytest, unittest) `assert` используется довольно активно:

```python
def test_math():
    assert 1 + 1 == 2
    assert len([1, 2, 3]) == 3
```

Pytest даже показывает информативные сообщения об ошибках при падении assert.
